# `src/persistence/postgres/` map

The sole structured-state implementation. Every adapter shares one bounded SQLx PostgreSQL pool,
uses tenant-qualified queries, database time for leases/deadlines, and explicit transactions for
claims and settlement. Serving startup accepts finalized schema 26 or the exact receipted 24/26
expand during the ADR-0041 mixed-fleet window. The additive v27 activation contract keeps
reconciliation egress dark through install/drain, attaches legacy/deletion guards at signed
Draining, and enables topology publication only from a signed Active generation with exact
fleet-image, source-completeness, and provider-contract fences. Pause is forward-only. The
one-release v0.9.31 Paused/g4 readiness bridge and its corrected Draining/g5 twin are retired.
Serving admits a verified Active or Paused chain for any activation-capable image: since
ADR-0046 the running model, location, and producer contract are the reviewed image's own,
registered at startup and bound into every claim, while the chain's recorded producer is
signed history.
Only the explicit
release migrator applies the append-only files under `migrations/`.

| File | Responsibility |
|---|---|
| `speaker_identity.rs` / `speaker_identity.sql` | Shared current Me/name/lettered-slot presentation, provenance and person links; first-speech slot allocation, cluster/profile promotion and successor inheritance. Deletion-fenced preparation compares graph/projection signatures in bounded transactions; participant reads use the same graph snapshot and structural legacy fallback. |
| `speaker_writer_contract.rs` | Real PostgreSQL formation and signed-Active reconciliation contracts proving synchronous slot persistence, retained/successor stability, canonical organizer input and unchanged source commitments. |
| `speaker_query_contract.rs` | Real PostgreSQL canonical label/participant parity across read surfaces, historical speaker-filter preparation, snapshot consistency, playback, export and tenant isolation. |
| `voice_worker_contract.rs` | Test-only real PostgreSQL/encrypted fake-GCS serving exercise, decoded-cache reuse and provider-free missing-media expiry. |
| `voice_identity.rs` | Bounded retention-gated voice leases, capture-session then stable account-wide continuity, three-clean-observation stability, robust representatives, profile/person conflict quarantine, and same-transaction sample-erasure recomputation with historical biometric-payload redaction. Voice attachment, person propagation, quarantine and erasure refresh all affected memory speaker projections, including cached links after source purge. Planned v27 deletion inventory fences voice writes across member-purge gaps; legacy-safe catalog probes and monotonic counter bootstrapping preserve older lineage. Provider-free maintenance erases up to500 expired/withdrawn samples regardless of Pause/cohort, then reconsiders up to16 retained ready samples in a fair queue under locked controls, using the same matching path without new inference. |
| `mod.rs` | TLS PostgreSQL pool construction, UTC/statement-timeout policy, schema marker primitives, fresh current-schema catalog presence probes, disposable test ladder, and shared transaction helpers. |
| `catalog_presence_tests.rs` | Synthetic cached-connection absence/install/drop visibility after an actual cross-connection release-lock wait; wrong-kind activation/erasure relations must fail closed. No owner data. |
| `aggregate_audit.rs` / `aggregate_audit.sql` / `aggregate_audit_fixture.json` | Fixed, content-free v6 PostgreSQL aggregate snapshot in one repeatable-read/read-only transaction. Preserves raw unfinished-source diagnostics, prior source-isolation counts and all provider/lease, quota, capacity, reconciliation, finalization and activation gates. Fresh-work counts remain distinct from full context owner bounds; successor capacity includes nonfresh context atoms once per account from the same complete graph, excluding disconnected history. Owner eligibility requires the shared source-closed runtime proof; it does not claim unfinished captures are complete or restored. Historical v2–v5 evidence must not be relabeled v6. |
| `activation_source_isolation.sql` | Bounded metadata-only source/draft closure for owner-prelaunch activation while retaining v0.9.24 serving safety. Includes event and projection intervals, unowned/draft-owned atoms and non-temporal member ownership; refuses held-source cross-session families, recent/open/unaccounted debt, empty/conflicting candidates and inventory overflow. No IDs, content, source completion, mutation, erasure or runtime bypass. |
| `schema_release.rs` | Session-locked online v24-to-v26 release plus phase-aware v27 serving verification: marker-preserving expansion, exact per-step catalog evidence, concurrent indexes, baked-anchor Ed25519 fleet/activation authorization, and activation-capable readiness for the reviewed image (ADR-0046). |
| `activation.rs` | Append-only v27 install/backfill/drain/activate/pause/resume authority, shared/exclusive release-lock serialization before schema probes, sticky scope, durable candidate-fleet image identity, exact catalog/receipt verification, database guard installation, claim drain, and runtime/repository gates. Installed backfill clears bounded expired terminal media ownership only for quiescent accounts. Draining backfill materializes bounded signed-scope assignments even for empty accounts; a distinct migrator repair accepts only the exact already-committed Draining authority and complete current-generation ledgers. Lifecycle fencing preserves deletion/admission; no history, quota, content, signed event, or schema identity is rewritten. Its isolated contract proves frozen historical verification before installing the current email, brief and voice companions for current delivery checks. |
| `activation_epoch.rs` / `activation_epoch_catalog.sql` | One-time signed v1 Draining/g1 to v2 Draining/g2 upgrade, frozen-history verification, rollback-only catalog proposal with a distinct physical readback connection, and exact namespace evidence including attached indexes and standalone types. Synthetic PG tests reject partial/tampered catalogs, wrong prior authority, partial commit and replay, prove historical bytes remain unchanged, and exercise partial providerless KEEP. The internal transition method is migrator-only; it must not become reachable from serving routes. New CLI Draining-to-Active requires ordinary v2 g3 or later. |
| `admission.rs` | Fleet token buckets and crash-recoverable concurrency leases. |
| `billing.rs` | Creating and lookup-only billing pseudonym resolution, recording lease/credit receipts, coverage anchors, retained-account metrics, and detach work. |
| `capture_enrollment.rs` / `capture_enrollment_contract.rs` | First accepted-event owner designation, withdrawal revision and sticky session violations after replay checks; ordinary audio receipt preservation, bounded screen-batch refusal and session status metadata. Real PostgreSQL admission regressions cover out-of-order/replay, offline withdrawal, stream/device scope, timeline rebasing and the batch race. |
| `voice_enrollment_schema.rs` | One direct v31 companion install and exact owned-catalog receipt verification for owner person uniqueness, per-observation owner links and enrollment withdrawal metadata. |
| `voice_enrollment.rs` / `owner_enrollment_policy.rs` / `owner_voice.rs` | Owner status, Forget and terminal enrollment progress; ordered three-minute speech dominance, cutoff-contained enrollment samples, deferred owner promotion until validated closure, revocation-fenced per-turn matching, complete retained-support candidate holds, and mixed-cluster update exclusion with surviving-evidence recognition. General expiry clears cluster fallbacks before refreshing projections. |
| `voice_enrollment_contract.rs` | Real PostgreSQL owner enrollment, matching, withdrawal, partial expiry, bounded-cleanup backlog and public-label regressions. |
| `capture.rs` | Atomic capture/reference admission, media metadata, provisional finish receipts, append-audited seal-generation reopen for late offline sources, exact deletion-tombstone replay acknowledgement without resurrection, event/session status, and replay. |
| `capture_recovery.rs` / `capture_recovery_tests.rs` | Bounded interrupted-session recovery after 30 minutes of database-receipt inactivity; activation/lifecycle/source/account locks, upload/deletion/contiguity/media holds before the batch limit, truthful provisional finish at the captured horizon, and ordinary late-source/seal semantics. Synthetic PostgreSQL tests cover no-finish formation/sealing, old timestamps, replay, out-of-order late uploads, bounded recovery, and deletion races. |
| `interrupted_capture_schema.rs` | Dedicated migrator installation and mandatory exact v29 companion receipt for the server-inactivity provenance CHECK. Reconstructs only that verified constraint in historical v27 catalog evidence, preserves signed history, and refuses missing/tampered companions or unrelated schema drift. |
| `delivery_outbox.rs` | Webhook/APNs outboxes and common provider lanes; email delegates to the daily source-coverage scheduler. |
| `morning_email.rs` | Local-calendar 7 a.m. schedules, bounded settled-brief selection, source/lineage coverage, exact frozen daily envelopes, retry/ambiguity, consent and deletion recovery. The schedule timezone follows the recording device (newest capture event) unless a settings save is more recent. |
| `morning_email_schema.rs` | Explicit additive v28 companion installation and exact DDL/catalog receipt verification, including all four provider/deletion/recipient trigger definitions and attachments; preserves v26/v27 markers. Populated PostgreSQL cutover tests prove quiescence, consent and source-coverage transfer, predecessor refusal and legacy-send fencing. |
| `entitlement.rs` | Active-account checks and atomic daily quota/Vertex reservations. |
| `episode_deletion.rs` | Durable logical freeze plus uncapped keyset-paged member/root/family/session inventory, canonical/reference-family and provider-claim fences, bounded GCS acknowledgement before structured mutation, reference-before-root accepted-sequence tombstone/purge, media aggregate cleanup/replan, formation refresh, exact terminal closure, and replay receipt. |
| `finalization.rs` | Four-hour source-settled complete-brief claim/egress/settlement with canonical current speaker input and independent initial webhook/APNs events plus morning source eligibility. |
| `identity.rs` | Accounts, provider identities, signup budget (the creating call learns the day's count once its transaction commits), Apple credentials/grants, and coherent session reads. |
| `lifecycle.rs` | Pre-fence account admission tombstones, deletion-owned recovery of expired outbound claims/global lanes, deletion ownership/progress, transactionally marked reviewer-fixture refusal, revocation settlement, cascading purge, and no-resurrection checks. |
| `media_processing.rs` | Media work claims, attempts, usage, screen/audio projection with stable owner-source classification, voice jobs and synchronous accepted-name projection refresh, database-time bounded retry/resurrection, retention progress, and transactional pre-egress recovery of exact legacy budget-retry aggregates; malformed work with retained or ambiguous provider evidence is held without replay. |
| `memory_formation.rs` | Forward summarizer windows plus exact late-session formation revisions and durable bounded pages, frozen request/attempt recovery, reference-aware unowned source projection, evidence-free deletion tombstones, renewed durable provider/deletion fences, pending-revision topology rebind, bounded legacy-finish import, four-hour quiet seal generations, episode/member writes with atomic stable speaker slots in both settlement lanes, canonical unowned speaker evidence, complete memory/final-brief human-text embeddings, and atomic durable settlement. |
| `memory_reconciliation.rs` | PostgreSQL current-formation source closures and fingerprints, prompt eight-hour complete owner-context selection with held-component traversal and separate four-hour brief settlement, exact-set active-generation claims and producer-bound provider egress/stages, bounded providerless oversized-neighborhood discovery plus independent commitment verification, serializable topology publication with predecessor slot transfer before retired-draft cleanup and refresh of every retained/new successor, and bounded handle resolution. Organizer labels are applied after unchanged raw source fingerprints are computed. |
| `reconciliation_source_components.sql` / `reconciliation_source_components_tests.rs` | Shared content-free source graph and synthetic real-PostgreSQL regressions: whole session horizons, eligible projections, active draft/finalized header/member intervals and cross-session canonical bridges. Complete bounded inventory precedes both lanes and exact-set snapshot revalidation; held traversal is capped at eight components. Truncated, empty-predecessor, conflicted or over-bound components never authorize provider work. Empty headers remain graph bridges but hold their entire component. Oversized KEEP retains the full owner graph while advancing only the unfinished draft prefix, so later pages cannot repeat already-kept owners. |
| `reconciliation_source_audit.rs` | Count-only v6 activation proof using that same source graph and sweep bound. Reports fresh draft counts while bounding every context owner, and supplies private per-account context-source capacity to the fixed aggregate without changing its public shape. Requires every current candidate component to fit normal runtime bounds and every account's components to be reachable within one bounded sweep; unfinished sources remain held, with zero staged/processing/retry formation work. It is valid only with the corresponding source-closed serving release, never the v24 runtime. |
| `model_usage.rs` | Paid-model intents/outcomes, usage batch claims/delivery, and coverage reconciliation. |
| `notification.rs` | Webhook destinations, explicit email consent/timezone rescheduling, push installations and configuration/disclosure-fence serialization. |
| `oauth.rs` | OAuth registration, consent, authorization codes, native sessions, and refresh-token rotation. |
| `orphan_capture_erasure.rs` / `orphan_capture_erasure_catalog.sql` | Independently cataloged erased-session/stream/event/asset admission before billing and under the upload account lock. Exact schema remains mandatory for this image's serving startup and capture admission. Compatible migrator activation alone permits verified whole-namespace absence; partial/tampered namespaces and pending operations fail closed. Only the signed migrator may install it. |
| `orphan_capture_erasure_audit.rs` / `orphan_capture_erasure_audit.sql` | Content-free erasure counts and separate quiescent, complete-fenced, restored and activation-clear gates in the v5 snapshot. Verified absence is activation-clear for predecessor owner-prelaunch only, never complete or unfenced. Public qualification still requires installed, complete and unfenced. |
| `orphan_capture_erasure_tests.rs` | Real-PostgreSQL erasure-schema tamper/absence, sealed-inventory, canonical-object, billing/upload-fence, tenant isolation, immutable replay identity, completion/scrub, late projection and whole-account cascade tests. No production data. |
| `orphan_capture_erasure_authority.rs` | Strict canonical signed domains for neutral install-only, exact-target inspection/preparation, provider acknowledgement, finalization and post-Active fence release; release-key verification, database-time freshness, exact target/account provider-name commitments, and content-free errors. Private target JSON is never retained in the permanent ledger. |
| `orphan_capture_erasure_scope.rs` / `orphan_capture_erasure_scope.sql` | Fixed account/session-qualified orphan closure, cross-family/projection/identity/browser/work refusal, zero retained worker/upload authority, protected revision-zero handle proof, and PostgreSQL-local source/survivor commitments. Raw content is never returned to the operator. |
| `orphan_capture_erasure_operator.rs` | Explicit signed migrator dispatch, server-enforced transaction/statement/idle/lock deadlines and synchronous WAL settings, nonwaiting activation/lifecycle/reconciliation/retention/account locks, exact request replay, inventory-before-cascade preparation, atomic provider-acknowledged completion/scrub, and post-new-Active fence release. No provider I/O or private identifiers in results/errors. |
| `orphan_capture_erasure_operator_tests.rs` | Synthetic real-PostgreSQL deadline rollback, exact two-session/three-stream signed inspection, shared/protected refusal, stale-scope rollback, source/provider inventory transfer, exact-session playback refusal after preparation, old-serving capture fence, strict replay, provider acknowledgement and atomic completion tests. No production content or provider I/O. |
| `playback.rs` | Tenant-qualified memory/session timelines, pre-transcription exact encrypted-source availability, late-event bounds, deletion fences and current graph-derived identified/recurring-person projections in one read snapshot. Memory and session labels share canonical identity/slot derivation. |
| `query.rs` | PostgreSQL full-text/pgvector retrieval and hybrid fusion across memories, final briefs, transcripts, and screen evidence; batched stable memory/identified-or-recurring-person navigation; turn-timed episode members; feed/people/evidence projections; MCP query shapes; and capture status. Speaker filtering, enrichment, participant details and export share canonical identity derivation; projection preparation precedes read-only snapshots. People list/detail/evidence/statements share metadata, public eligibility and enrichment in one repeatable-read snapshot; list lookahead is trimmed before enrichment. |
| `recording_retention.rs` | Retention preview/CAS, key epochs, exact media inventory, and downgrade reconciliation. |
| `voice_recurrence.rs` | Versioned recurring-person promotion from stable retained attribution across current memories or union speech duration, with complete existing-public support withdrawal independent of bounded promotion candidates and opaque-ID reuse before canonical projections. Recurrence summaries use the caller’s read-only People snapshot for support, contexts and co-participants. |
| `voice_profile_reconciliation.rs` | Account-locked exact profile proposals, append-only assignment/revision provenance, letter reservation transfer and guarded reversal; bounded current-policy adoption without provider inference. |
| `voice_recurrence_schema.rs` | Direct v32 proposal/member/slot companion receipt, exact owned-catalog readiness and prior-schema preservation. |
| `voice_identity_schema.rs` | Exact v30 SQL/catalog receipt for operator cohort/pause controls, sample uniqueness and claim index; dedicated migrator install/mutations, default-off seed, bounded stable-UUID scope and serialized revisions. Startup/readiness are read-only. Controls and receipt are excluded from fixture truncation. |
| `work.rs` | Fleet active-account enumeration and PostgreSQL-backed summarizer cursor storage. |

No adapter reads a filesystem or GCS database, and no route can choose a structured-state
implementation. Real PostgreSQL contracts—not alternate-backend parity—are the release authority
for tenant isolation, type/time-zone behavior, search, concurrency, restart, export, and deletion.

`brief_sections_schema.rs` installs/verifies the additive v29 sections column and receipt without modifying base/activation catalog contracts. Finalization writes sections atomically; query/search/embedding and immutable delivery snapshots preserve them.
