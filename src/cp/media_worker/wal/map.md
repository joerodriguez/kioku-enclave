# map.md — `src/cp/media_worker/wal/`

Private ADR-0022 logical-result children. Nothing in this directory
is a launcher, worker, provider adapter, Store policy, route, task, retry loop,
or acknowledgement path.

| File | Role |
|---|---|
| `claim.rs` | **Media work claim boundary (ADR-0022 slice 10i):** the missing durable boundary every settle family downstream already anchors on. It re-runs the identical bounded eligibility query the owner enumerated with (one definition, shared by construction), re-derives the member set through the same pure planner, and requires exact equality with the carried set before writing; the identity is the resolved member set plus every observed `media_work_units` field used by the constructor and the carried total-attempt cap, never the clock. Rows whose capture stamps or predecessor fields violate the settle bounds, including job attempts already at/above that cap, are named as `Unplannable` before any reservation/provider call; a non-empty enumeration whose deterministic head fits no planner window is also named rather than collapsed into `Idle`. A structured classifier names exact durable member, duplicate-event, or stored-work-unit evidence (malformed, future, exhausted attempt count, or mismatched immutable class/version/window/token topology), while global account/constants/clock/code failures remain log-only and non-charging; the plan-wide backward-clock check precedes member timestamp attribution. Construction and transactional re-resolution share the cap; job/unit increments use checked Rust values and guarded integer SQL assignments, never raw arithmetic. `lease_until` survives only as a deterministic derived recovery deadline (`committed_at + 300s`) — never renewed, no holder id, no fencing token — because it is the sole path back for a unit abandoned mid-flight and because the sealed `result`/`audio_result` settles CAS on its exact byte value. `created_at`/`updated_at` are bound explicitly so no `strftime` DEFAULT can fire inside `apply()`. **`scan_for_claim` carries the T24 audio gate: it checks all six v3 target allocators (`audio_segments`, `speaker_clusters`, `speaker_observations`, `utterances`, `voice_embedding_jobs`, and `identity_evidence`) and opens only on the sealed `AUTOINCREMENT` baseline. A missing table or legacy plain-primary-key shape therefore refuses before reservation/provider work. Virgin `AUTOINCREMENT` tables need no existing `sqlite_sequence` row.** No provider call, no media read, no clock, no Store, no launcher, no retry loop. |
| `quarantine.rs` | **Deterministic claim-refusal quarantine:** the bounded response to a named claim refusal. It commits every column in the complete joined claim rows plus all existing-work-unit predecessor and expected immutable-topology fields when applicable, sorts members by job id for canonical multi-row identity, transactionally rereads and re-derives all evidence before the first write, and rejects repaired/stale plans with no mutation. It covers malformed/unwindowable members, member commit order, duplicate-event topology, and malformed/future/derived-mismatched existing work units. One transaction advances attempts without overflow, parks or terminalizes jobs on the carried retry/resurrection bounds, settles an evidenced work unit, and applies one aggregate media disposition per event so duplicate jobs cannot self-invalidate. Replay is permanent and exact. It cannot create a work unit or reservation, read media, call a provider, allocate a clock/id, launch, or retry. |
| `failure.rs` | **Media work failure tail (ADR-0022 slice 10i):** one plan, one transaction, over the whole legacy tail — per-job `mark_failed` or `defer_for_budget` plus the work-unit state/error update — each write a full-tuple CAS on the carried predecessor. `attempt_limit`, `resurrection_window_seconds` and `budget_retry_seconds` are carried, never read from a code constant inside `apply()`. **In both arms the non-terminal branch writes the FUTURE retry time into `updated_at`, not the commit stamp:** `updated_at` doubles as next-attempt-at for every eligibility predicate on this lane, so binding it to `committed_at` would destroy the exponential backoff and turn a transient Vertex outage into a paid retry storm. `error_code` is part of the identity. No provider call, no clock, no Store, no launcher, no retry loop. |
| `audio_attempt.rs` | **Inactive audio-window Vertex-begin B boundary (ADR-0022 slice 11):** the structural clone of the sealed screen attempt boundary under distinct audio identity domains and its own `archive_v3_wal_audio_vertex_attempt_*` ledger. It authenticates one complete reserved audio-work attempt (member cap 128 — the lease scan LIMIT, never the 12-frame screen cap) with at least the fixed two-minute provider window, derives the clock-free event ID from account/work/attempt-topology identity, and atomically records the exact `audio_understanding` started billing event, deterministic full-tuple monthly coverage advance, complete work binding, and bounded typed receipt. Same-attempt replay is exact while a renewed lease/counter topology (including `audio_role`/`audio_route`/`route_epoch` moves) yields a new identity. No provider/media read, clock/random ID, Store, worker, launcher, task, retry, or acknowledgement authority. |
| `audio_result.rs` | **Active audio-window transcript A-domain (ADR-0022 slice 11, bound-only):** derives its v3 subtype-separated operation from the terminal audio Vertex attempt, reauthenticates the sibling B ledger, binding, and stable work-attempt topology, authenticates the complete terminal predecessor (silent zero-turn windows are legal; the text cap counts chars, never bytes), asserts six fingerprinted allocator pins, and atomically inserts the audio segment, clusters, observations with exact source projections, utterances, and one fixed-id pending embedding job per turn while settling the jobs/media/work unit. Its only identity slot is a high-confidence same-turn self-identification whose claimed name and every carried fact-evidence string occur literally in that exact transcript. That slot writes one fixed-id unbound `proposed` identity row; it cannot create or mutate people, profiles, facts, claims, or bindings. Its 1,048,576-row/32-MiB ledger retains permanent unit replay. Provider calls, media reads, clocks, automatic IDs, Store, launching, retries, and acknowledgement remain absent. |
| `voice_embedding.rs` | **Active selected voice-embedding boundary:** first performs a bounded provider-free repair for historical v1 transcript observations that lack both sample and job, carrying the exact eligible observation, absence counts, and fingerprinted `voice_embedding_jobs` allocator before inserting one fixed-ID pending job. Existing work and policy abstentions are untouched, and the repair is exact-replayable after reopen. The active lane then exact-reads one bounded due job, its observation, raw source cardinality plus the complete projected source/media topology, installed wrapped media DEK commitment, existing-sample topology, and fingerprinted `voice_samples` allocator. Missing source children, duplicate prior samples, deterministic poison, pruned media, exhausted attempts, and invalid sequence state resolve provider-free; a global backward clock defers without charging or mutating the row. Authorized work durably claims with a checked attempt before any current-generation GCS read, KMS unwrap, strict-v2 decrypt, bounded 30-second decode, or in-enclave WeSpeaker inference. Settlement reauthenticates the exact claim/source/allocator evidence and writes only a caller-fixed pending sample (`accepted=-1`) or typed quality/retry/terminal result. Its 1,048,576-row/32-MiB ledger stores fixed-size result receipts, not transcript, media, DEK, diagnostic, or vector bytes. The active provider-free profile/person owner consumes the pending sample and any literal identity proposal afterward. The plan cannot call Store/GCS/KMS/model code, allocate a clock/random/profile/person ID, launch, or retry. |
| `voice_profile.rs` | **Active selected provider-free voice-profile/person boundary:** observes one exact bounded unit of historical revision backfill, sample-assignment backfill, pending-sample assignment, representative reconciliation/quarantine, literal person identity, imported lineage-action refusal, or episode speaker status. Profile decisions commit the relevant rows plus exact allocator pins; generated IDs and the mutation stamp are caller-fixed. New matching considers at most 32 compatible profiles and 100 active samples per profile. Once one proposed identity observation has exactly one accepted active profile, the person decision commits the complete bounded identity, transcript/source, observation, sample/assignment, profile/revision, cluster, person/name/fact and active-binding rows plus four person allocators. It revalidates same-turn literal evidence, conflicting names/bindings and every target before atomically creating or reinforcing one identified person, accepted name claim, bounded facts and one active superseding binding; corruption terminal-rejects without partial public state. Historical repairs precede new work, a backward clock defers byte-for-byte, and replay is permanent through a 1,048,576-row/32-MiB commitment-only ledger. No Store/provider/media/model/random clock/launcher/retry authority. |
| `attempt.rs` | **Inactive screen-storyboard Vertex-begin B boundary:** authenticates one complete reserved screen-work attempt with at least the fixed two-minute provider window, binds both its exact predecessor and post-usage-stable attempt identity, derives the event ID from account/work/attempt identity, and atomically records the exact started billing event, deterministic full-tuple monthly coverage advance, complete work binding, and bounded typed receipt. Same-attempt replay is exact while a renewed lease/counter topology yields a new identity. It has no provider/media read, clock/random ID, Store, worker, launcher, task, retry, or acknowledgement authority. |
| `result.rs` | **Inactive screen-storyboard result A-domain:** its production-facing v2 request derives a subtype-separated opaque operation from an already durable terminal Vertex attempt, reauthenticates the sibling B ledger and typed receipt, compares that binding's stable attempt commitment to the current leased terminal work, and authenticates the complete exact predecessor. The predecessor now includes each member's exact browser-v2 observation/state row. Before the first write it resolves only a context-matching event observation and binds `capture-v2-browser:<event_id>` into the screenshot; missing, repaired, or mismatched evidence refuses without a dangling link. It atomically inserts only caller-ID-fixed screenshots and screen observations while full-tuple settling their jobs, media rows, and work unit, then exact-reads its distinct 1,048,576-row/32-MiB ledger before commit. Replay reauthenticates the permanent attempt binding. Historical v1 identity/encoding remains test-covered behind a test-only constructor. The subtype accepts no person evidence and cannot create audio, identity, person, or voice rows. Provider calls, media reads, clocks, automatic IDs, Store, launching, retries, and acknowledgement remain absent. |
