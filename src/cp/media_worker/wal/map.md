# map.md — `src/cp/media_worker/wal/`

Private, inactive ADR-0022 logical-result children. Nothing in this directory
is a launcher, worker, provider adapter, Store policy, route, task, retry loop,
or acknowledgement path.

| File | Role |
|---|---|
| `audio_attempt.rs` | **Inactive audio-window Vertex-begin B boundary (ADR-0022 slice 11):** the structural clone of the sealed screen attempt boundary under distinct audio identity domains and its own `archive_v3_wal_audio_vertex_attempt_*` ledger. It authenticates one complete reserved audio-work attempt (member cap 128 — the lease scan LIMIT, never the 12-frame screen cap) with at least the fixed two-minute provider window, derives the clock-free event ID from account/work/attempt-topology identity, and atomically records the exact `audio_understanding` started billing event, deterministic full-tuple monthly coverage advance, complete work binding, and bounded typed receipt. Same-attempt replay is exact while a renewed lease/counter topology (including `audio_role`/`audio_route`/`route_epoch` moves) yields a new identity. No provider/media read, clock/random ID, Store, worker, launcher, task, retry, or acknowledgement authority. |
| `audio_result.rs` | **Inactive audio-window transcript A-domain (ADR-0022 slice 11, bound-only):** derives its subtype-separated operation from the terminal audio Vertex attempt, reauthenticates the sibling B ledger, binding, and current stable work-attempt topology, authenticates the complete exact terminal-stage predecessor (silent zero-turn windows are legal; the text cap counts chars, never bytes), asserts the four fingerprinted `sqlite_sequence` pins, and atomically inserts the audio segment, speaker clusters, speaker observations with exact source projections, and utterances at pin-derived ids (plain INSERT + derived-id assert) while full-tuple settling the exact jobs, media rows, and work unit. It cannot create identity, person, or voice rows — speaker names, person facts, and quality flags have no constructor slot, `voice_embedding_jobs` is never written, and every label is the proven owner-or-unidentified closed form. Its distinct 1,048,576-row/32-MiB ledger retains permanent unit replay. **Activation is BLOCKED by its own sequence gate (T24): the frozen epoch-0 baseline creates `audio_segments`/`utterances` without AUTOINCREMENT, so the pin discipline fails closed on a production archive until a reviewed schema treatment lands.** Provider calls, media reads, clocks, automatic IDs, Store, launching, retries, and acknowledgement remain absent. |
| `attempt.rs` | **Inactive screen-storyboard Vertex-begin B boundary:** authenticates one complete reserved screen-work attempt with at least the fixed two-minute provider window, binds both its exact predecessor and post-usage-stable attempt identity, derives the event ID from account/work/attempt identity, and atomically records the exact started billing event, deterministic full-tuple monthly coverage advance, complete work binding, and bounded typed receipt. Same-attempt replay is exact while a renewed lease/counter topology yields a new identity. It has no provider/media read, clock/random ID, Store, worker, launcher, task, retry, or acknowledgement authority. |
| `result.rs` | **Inactive screen-storyboard result A-domain:** its production-facing v2 request derives a subtype-separated opaque operation from an already durable terminal Vertex attempt, reauthenticates the sibling B ledger and typed receipt, compares that binding's stable attempt commitment to the current leased terminal work, and authenticates the complete exact predecessor. It atomically inserts only caller-ID-fixed screenshots and screen observations while full-tuple settling their jobs, media rows, and work unit, then exact-reads its distinct 1,048,576-row/32-MiB ledger before commit. Replay reauthenticates the permanent attempt binding. Historical v1 identity/encoding remains test-covered behind a test-only constructor. The subtype accepts no person evidence and cannot create audio, identity, person, or voice rows. Provider calls, media reads, clocks, automatic IDs, Store, launching, retries, and acknowledgement remain absent. |
