# map.md — `src/cp/query/wal/`

Private ADR-0022 query mutation children. With the explicit active episode
deletion exception below, they cannot call Store,
allocate attempts or clocks, launch work, schedule retries, invoke providers,
or acknowledge routes. The former selected-screenshot upload route is now an
authenticated Genesis `410 Gone` tombstone before request-body or archive work,
so the entire attempt/candidate/`SendStarted`/provider/A/C chain has no
production caller.

The parent selected-screenshot A codec's strongest typed contract consumes the
exact provider-accepted proof plus the complete execution-claim, send,
candidate, media-DEK, and permanent-attempt chain in a typed v3 request; its
historical unbound v1 and B-bound v2 construction is test-only. Exact replay and
restart reauthenticate that full chain without provider I/O. A second child authenticates that attempt
and the installed media-DEK receipt before retaining one exact context-bound
ciphertext candidate. A third child consumes only the exact-name,
DEK-authenticated candidate and durably records one deterministic `SendStarted`
request identity before any future provider call. A fourth provider-neutral
child can issue only one injected conditional create and one bounded exact-name
readback, returning non-cloneable success or definitive-no-object proof while
leaving unknown/collision outcomes unsettled. Before it returns the sole owned
request, a bounded durable execution claim prevents a second provider attempt.
A fifth child consumes only the exact definitive-no-object proof and execution
claim, retains an exact C terminal, releases the episode reservation for a
different target, and permanently fences the original target. Provider-accepted
A settlement is present only behind this WAL-private boundary; no production
caller or launcher exists for any selected-screenshot link.

| File | Role |
|---|---|
| `episode_delete.rs` | Active selected-archive episode deletion with three sealed phases. `EpisodeDeletePrepare` hashes the complete immediate tombstone/cascade closure (including cross-episode membership, delivery sidecars, and screenshot `SET NULL` survivors), removes user-visible plaintext, reserves the permanent receipt, and stores only compact ordered event/owned-voice/legacy selectors. `EpisodeDeleteCleanup` expands one selector under shared row/byte/query limits. A voice selector first reserves its exact 16,384-row legal maximum cleanup budget before any identity or provider-backed local mutation; expansion atomically swaps that reservation for the exact inventory. First-time current progress rows are separately charged to the global ledger before identity mutation, exact overwrites add no charge, and expansion releases the authenticated count. Other selectors exact-reserve at expansion before mutation. The family authenticates the capture/voice/allocator closure, gives imported pre-lineage samples scoped fixed-stamp revision/assignment backfill, preserves canonical ancestors, NULL-linked audio and all surviving shared roots, advances affected identity work through rotating 128-episode pages, and advances exact provider items durably. Voice churn overwrites one authenticated current progress row per affected episode rather than appending page history; the rotating cursor reaches higher IDs before wrapping and those temporary rows disappear with local expansion. Selector finish releases exact dynamic capacity while retaining permanent inventory/result fingerprints; completion recomputes their gap-free aggregate. The dedicated immediate/30-second owner advances a durable four-episode cursor before work, uses bounded/coalesced non-biased wakeups, and applies a capped account-local failure delay while continuing ready neighbors; the summarizer is a redundant wakeup and the route returns 202 for a durable logical deletion still physically cleaning. `EpisodeDelete` records completion only after every selector and its authenticated provider inventory are complete. Receipt loading derives both operation IDs and fingerprints from account+episode and authenticates the exact result/source/selector cleanup commitments. The ledger retains no transcript, OCR, browser title/URL/tab, voice embedding, endpoint/secret/body, or finalization body. |
| `finalization_queue.rs` | Closed caller-stable finalization-queue codec and distinct bounded permanent unit-replay ledger. It fingerprints the complete eligible episode predecessor plus a fixed request identity and canonical queue timestamp, then full-tuple transitions only that exact row to `queued`. |
| `selected_screenshot_attempt.rs` | Inactive pre-provider B boundary that consumes one caller-fixed opaque attempt ID, reauthenticates the complete eligible screenshot predecessor and exact numeric screenshot ID, derives the account-bound object key, atomically reserves pending episode count/bytes, and retains the full binding plus typed receipt. Exact local consumption avoids double-counting; only a fully authenticated exact C terminal releases absent/inexact consumption for another target. It has no random/clock/DEK/media/provider/Store/launcher/retry/rejection-release/cleanup/acknowledgement authority. |
| `selected_screenshot_provider.rs` | Inactive provider-neutral exact-create/readback boundary. Preparation requires the exact-name, DEK-authenticated `SendStarted` payload and exact installed wrapped DEK. One immediate transaction admits a unique bounded full-binding execution claim before returning the sole owned request; duplicates or lost requests are fail-closed/manual, never retry authority. Its injected seam exposes only one conditional create and one bounded exact get; there is no retry/list/delete. Exact bytes, metadata, request identity, and positive generation mint a non-cloneable accepted proof. Only an explicitly definitive no-create plus exact absence mints a distinct non-cloneable rejection proof. Unknown/unavailable absence stays unknown and collisions or malformed provider facts become manual. It has no concrete transport, GCS/Store/KMS client, clock/randomness, A/C mutation, route, task, launcher, cleanup, or acknowledgement authority. |
| `selected_screenshot_send.rs` | Inactive `SendStarted` continuation. Its plan can be built only from the WAL-private exact-name candidate loader, derives a deterministic 256-bit send request ID and complete send binding, requires the exact candidate plus an unconsumed B attempt on first apply, and atomically inserts a separate bounded marker before any future provider I/O. Replay may survive later exact A settlement. The parent can obtain only an opaque plan from a WAL-owned factory. Its post-marker exact-name loader, payload, receipt access, and ciphertext getter also remain WAL-private; only the provider-proof child can consume them after requiring account, image, and borrowed DEK. It has no enumeration, KMS/provider client, network I/O, clock/randomness, retry, outcome classification, A settlement, Store, route, launcher, delete/list, or acknowledgement authority. |
| `selected_screenshot_termination.rs` | Inactive definitive-no-object C settlement. It can be constructed only from the non-cloneable rejection proof and caller-fixed canonical observation time. One immediate transaction reauthenticates the unique execution claim, complete B/candidate/`SendStarted`/provider chain, and absence of any local/A settlement before retaining a bounded unit terminal with exact readback. Exact replay/restart never retries the provider. Only the authenticated terminal releases episode count/bytes for another target; the original target remains burned, and tampered/partial state fails closed. It fences provider-accepted A-v3, new candidate admission, and provider request preparation, and has no provider/retry/Store/KMS/list/delete/clock/random/launcher/route/task/acknowledgement authority. |
| `selected_screenshot_upload.rs` | Inactive `CandidateReady` continuation of the exact selected-screenshot attempt. Construction borrows the validated JPEG and media DEK, authenticates the installed-DEK receipt, exact-decrypts the supplied context-bound ciphertext, and retains only that ciphertext plus keyed commitments in a 512-MiB-capped permanent ledger. Apply requires the exact unconsumed attempt; replay reauthenticates both predecessors. Its private exact-name restart loader preflights lengths and requires the account, attempt, and borrowed DEK before returning only that authenticated ciphertext/receipt. It has no enumeration, KMS, provider, send-start, outcome, Store, route, launcher, retry, delete/list, or acknowledgement authority. |
