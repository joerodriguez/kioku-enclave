# map.md — `src/cp/query/`

Private inactive ADR-0022 logical-operation children. They do not define routes
or obtain Store, provider, task, acknowledgement, or launcher authority.

| File | Role |
|---|---|
| `wal.rs` | Closed selected-screenshot receipt codec and distinct bounded permanent exact-replay ledger. A future B boundary must durably choose the opaque upload attempt and exact account-bound object before provider I/O; this child only atomically validates and records that exact receipt. Exact pre-existing attempt facts may be adopted, while another object or screenshot binding is rejected. |
